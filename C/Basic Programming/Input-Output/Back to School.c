#include<stdio.h>
int main()
{
    int a,b,c,d=0,i;
    scanf("%d %d %d",&a,&b,&c);
        if(d<=a)
        {
            d=a;
        }
        if(d<=b)
        {
            d=b;
        }
        if(d<=c)
        {
            d=c;
        }
        printf("%d",d);
    return 0;
}
